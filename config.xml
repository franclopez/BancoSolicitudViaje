<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "co.com.tcs.bancolombia.aprobacionviajes"
        versionCode="10"
        version   = "1.1.0">

    <!-- versionCode is optional and Android only -->

    <name>Anticipo de Viajes</name>

    <description>
     Bancolombia Anticipo de Viajes
    </description>

    <author href="http://www.tcs.com" email="frankolopez@gmail.com">
        Francisco Lopez
    </author>
	
	    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"           value="2.9.0" />          <!-- all: current version of PhoneGap -->
    <preference name="orientation"                value="default" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="true" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="7" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

	
	 <icon src="res/drawable-ldpi/ic_launcher.png"   gap:platform="android"    gap:density="ldpi" />
    <icon src="res/drawable-mdpi/ic_launcher.png"   gap:platform="android"    gap:density="mdpi" />
    <icon src="res/drawable-hdpi/ic_launcher.png"   gap:platform="android"    gap:density="hdpi" />
    <icon src="res/drawable-xhdpi/ic_launcher.png"  gap:platform="android"    gap:density="xhdpi" />
    <icon src="images/ic_launcher.png"     gap:platform="blackberry" />
    <icon src="images/ic_launcher.png"     gap:platform="blackberry" gap:state="hover"/>
    <icon src="res/drawable-hdpi/ic_launcher.png"            gap:platform="ios"        width="57" height="57" />
    <icon src="res/drawable-hdpi/ic_launcher.png"            gap:platform="ios"        width="72" height="72" />
    <icon src="res/drawable-xhdpi/ic_launcher.png"         gap:platform="ios"        width="114" height="114" />
    <icon src="res/drawable-xhdpi/ic_launcher.png"         gap:platform="ios"        width="144" height="144" />
    <icon src="images/ic_launcher.png"          gap:platform="webos" />
    <icon src="images/ic_launcher.png"  gap:platform="winphone" />
    <icon src="images/ic_launcher.png" gap:platform="winphone"   gap:role="background" />
	
    <!-- Define app splash screen for each platform. -->
    <gap:splash src="res/drawable-ldpi/splash.png"  gap:platform="android" gap:density="ldpi" />
    <gap:splash src="res/drawable-mdpi/splash.png"  gap:platform="android" gap:density="mdpi" />
    <gap:splash src="images/splash.png"  gap:platform="android" gap:density="hdpi" />
    <gap:splash src="images/splash.png" gap:platform="android" gap:density="xhdpi" />
    <gap:splash src="images/splash.png"         gap:platform="blackberry" />
    <gap:splash src="res/drawable-hdpi/splash.png"    gap:platform="ios"     width="320" height="480" />
    <gap:splash src="res/drawable-hdpi/splash.png" gap:platform="ios"     width="640" height="960" />
    <gap:splash src="res/drawable-xhdpi/splash.png"      gap:platform="ios"     width="768" height="1024" />
    <gap:splash src="res/drawable-xhdpi/splash.png"     gap:platform="ios"     width="1024" height="768" />
    <gap:splash src="images/splash.png" gap:platform="winphone" />
	 
	<!--
		Define access to external domains.

		<access />            - a blank access tag denies access to all external resources.
		<access origin="*" /> - a wildcard access tag allows access to all external resource.

		Otherwise, you can specify specific domains:
	  -->
	  
	  
	<access origin="*"/> <!-- allow local pages -->
	<access origin="http://127.0.0.1*" /><!-- allow local pages -->
	<access origin="https://baas.kinvey.com*" />
	<access origin="https://da189i1jfloii.cloudfront.net*" />
	<access origin="https://storage.googleapis.com*" />
	<access origin="https://baas.kinvey.com*" />	
	<access origin="https://build.phonegap.com*" />  

</widget>
